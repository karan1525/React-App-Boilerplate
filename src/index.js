// BASE BOILERPLATE FOR REACT

import React from 'react';
import ReactDOM from 'react-dom';
import './styles/styles.scss';

ReactDOM.render(
  <p>This is a boilerplate for SCSS and SASS</p>,
  document.querySelector('#root')
);
